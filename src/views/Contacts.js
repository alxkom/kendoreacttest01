import React from 'react';
import ImageMapView from '../components/ImageMapView';

const Contacts = () => (
    <div key="contacts-page">
        <h1>Contacts Page</h1>
        <ImageMapView id='01' />
    </div>
);

export default Contacts;